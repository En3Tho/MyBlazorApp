@attribute [Route($"/{nameof(TailWindTestPage2)}")]

@foreach (var component in new RenderFragment[]
{
    @<EmailForm/>,
    @<RadioButtons/>,
    @<RadioButtons2/>,
    @<AttributesChild class="!h-16 !w-16 !bg-white" @onclick="@(() => Console.WriteLine("abc"))"/>
})
{
    <ExampleBlock>
        @component
    </ExampleBlock>
}

@code {

}