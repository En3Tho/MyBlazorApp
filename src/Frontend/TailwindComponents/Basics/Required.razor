@Main
@If(Validator(), Error)

@code {

    [Parameter, EditorRequired]
    public required RenderFragment Main { get; init; } = default!;

    [Parameter, EditorRequired]
    public required RenderFragment Error { get; init; } = default!;

    [Parameter, EditorRequired]
    public required Func<bool> Validator { get; init; } = default!;
}