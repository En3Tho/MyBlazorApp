@Main
@if (Validator())
{
    @Error
}

@code {

    [Parameter, EditorRequired]
    public RenderFragment Main { get; init; } = null!;

    [Parameter, EditorRequired]
    public RenderFragment Error { get; init; } = null!;

    [Parameter, EditorRequired]
    public Func<bool> Validator { get; init; } = null!;
}