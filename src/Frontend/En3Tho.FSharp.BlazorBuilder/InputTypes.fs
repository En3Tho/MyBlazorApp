// Auto-generated
namespace En3Tho.FSharp.BlazorBuilder

open En3Tho.FSharp.BlazorBuilder.Core

[<AbstractClass; Sealed; AutoOpen>]
type InputTypes() =

    static member typeHidden' = Attribute<KnownAttributes.Type, _>("hidden")

    static member typeSearch' = Attribute<KnownAttributes.Type, _>("search")

    static member typeText' = Attribute<KnownAttributes.Type, _>("text")

    static member typeTel' = Attribute<KnownAttributes.Type, _>("tel")

    static member typeUrl' = Attribute<KnownAttributes.Type, _>("url")

    static member typeEmail' = Attribute<KnownAttributes.Type, _>("email")

    static member typePassword' = Attribute<KnownAttributes.Type, _>("password")

    static member typeDate' = Attribute<KnownAttributes.Type, _>("date")

    static member typeMonth' = Attribute<KnownAttributes.Type, _>("month")

    static member typeWeek' = Attribute<KnownAttributes.Type, _>("week")

    static member typeTime' = Attribute<KnownAttributes.Type, _>("time")

    static member typeDatetimeLocal' = Attribute<KnownAttributes.Type, _>("datetime-local")

    static member typeNumber' = Attribute<KnownAttributes.Type, _>("number")

    static member typeRange' = Attribute<KnownAttributes.Type, _>("range")

    static member typeColor' = Attribute<KnownAttributes.Type, _>("color")

    static member typeCheckbox' = Attribute<KnownAttributes.Type, _>("checkbox")

    static member typeRadio' = Attribute<KnownAttributes.Type, _>("radio")

    static member typeFile' = Attribute<KnownAttributes.Type, _>("file")

    static member typeSubmit' = Attribute<KnownAttributes.Type, _>("submit")

    static member typeImage' = Attribute<KnownAttributes.Type, _>("image")

    static member typeReset' = Attribute<KnownAttributes.Type, _>("reset")

    static member typeButton' = Attribute<KnownAttributes.Type, _>("button")
